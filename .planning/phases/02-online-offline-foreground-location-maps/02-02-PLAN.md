---
phase: 02-online-offline-foreground-location-maps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/_layout.tsx
  - src/app/(app)/index.tsx
  - src/app/(app)/vehicles/_layout.tsx
  - src/app/(app)/vehicles/index.tsx
  - src/app/(app)/vehicles/new.tsx
  - src/app/(app)/vehicles/[id].tsx
  - src/app/(app)/verify/_layout.tsx
  - src/app/(app)/verify/index.tsx
  - src/app/(app)/verify/[documentType].tsx
  - src/app/(app)/profile/_layout.tsx
  - src/app/(app)/profile/index.tsx
  - src/app/(app)/profile/edit.tsx
  - src/features/vehicles/components/VehicleListEmptyState.tsx
  - src/features/profile/components/ProfileView.tsx
autonomous: true
requirements: [PRES-01]

must_haves:
  truths:
    - "The bottom tab bar is gone — Home is the sole bottom-level screen after auth"
    - "Verification, Vehicles, and Profile are still reachable and functional from Home"
    - "Every router.push target in the app resolves under the new flattened route tree"
    - "TypeScript compiles with no stale (tabs) route references"
  artifacts:
    - path: "src/app/(app)/index.tsx"
      provides: "Top-level Home screen (was (tabs)/index.tsx)"
      min_lines: 40
    - path: "src/app/(app)/_layout.tsx"
      provides: "Stack layout with no (tabs) child, so new sibling routes auto-register"
      min_lines: 10
    - path: "src/app/(app)/vehicles/_layout.tsx"
      provides: "Vehicles nested stack, moved out from under (tabs)"
    - path: "src/app/(app)/verify/_layout.tsx"
      provides: "Verify nested stack, moved out from under (tabs)"
    - path: "src/app/(app)/profile/_layout.tsx"
      provides: "Profile nested stack, moved out from under (tabs)"
  key_links:
    - from: "src/app/(app)/index.tsx"
      to: "/verify, /vehicles, /profile"
      via: "router.push with flattened hrefs"
      pattern: "router\\.push\\('/(verify|vehicles|profile)'\\)"
    - from: "src/features/profile/components/ProfileView.tsx"
      to: "/profile/edit"
      via: "router.push"
      pattern: "router\\.push\\('/profile/edit'\\)"
    - from: "src/features/vehicles/components/VehicleListEmptyState.tsx"
      to: "/vehicles/new"
      via: "router.push"
      pattern: "router\\.push\\('/vehicles/new'\\)"
---

<objective>
Delete the `(tabs)` route group and flatten `index`, `vehicles/`, `verify/`, and `profile/` to
sit directly under `src/app/(app)/`, per CONTEXT.md's locked "Full switch to the design's
menu-based nav" decision. Screen internals are otherwise untouched — this plan is a pure
structural move plus the mechanical import-depth and route-literal fixes it forces.

Purpose: every later plan in this phase builds on the new route tree (D03 Menu at `/menu`,
D06 Home at `/`, the visual retrofits of `/verify` and `/vehicles`). RESEARCH.md Pitfall 2 —
already proven in this repo during Phase 01.1 — is that Expo Router's typed-routes declaration
file `.expo/types/router.d.ts` is only regenerated by the `expo start` dev-server file watcher,
never by `tsc` or `expo export`. Doing the move in isolation means a `tsc` failure afterwards is
unambiguously attributable to the restructure, not to new screen code layered on top.
Output: A flattened, tab-less route tree that compiles clean and navigates identically.
</objective>

<execution_context>
@/Users/shawonkanji/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawonkanji/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-online-offline-foreground-location-maps/02-CONTEXT.md
@.planning/phases/02-online-offline-foreground-location-maps/02-RESEARCH.md

<interfaces>
<!-- The exact before/after route tree. Executor should not need to explore to find these. -->

BEFORE (current, verified on disk):
```
src/app/(app)/_layout.tsx                     <- renders <Stack><Stack.Screen name="(tabs)" /></Stack>
src/app/(app)/(tabs)/_layout.tsx              <- renders <Tabs> with 4 Tabs.Screen entries
src/app/(app)/(tabs)/index.tsx
src/app/(app)/(tabs)/vehicles/_layout.tsx
src/app/(app)/(tabs)/vehicles/index.tsx
src/app/(app)/(tabs)/vehicles/new.tsx
src/app/(app)/(tabs)/vehicles/[id].tsx
src/app/(app)/(tabs)/verify/_layout.tsx
src/app/(app)/(tabs)/verify/index.tsx
src/app/(app)/(tabs)/verify/[documentType].tsx
src/app/(app)/(tabs)/profile/_layout.tsx
src/app/(app)/(tabs)/profile/index.tsx
src/app/(app)/(tabs)/profile/edit.tsx
```

AFTER (target):
```
src/app/(app)/_layout.tsx                     <- <Stack screenOptions={{ headerShown: false }} /> with NO explicit children
src/app/(app)/index.tsx
src/app/(app)/vehicles/_layout.tsx
src/app/(app)/vehicles/index.tsx
src/app/(app)/vehicles/new.tsx
src/app/(app)/vehicles/[id].tsx
src/app/(app)/verify/_layout.tsx
src/app/(app)/verify/index.tsx
src/app/(app)/verify/[documentType].tsx
src/app/(app)/profile/_layout.tsx
src/app/(app)/profile/index.tsx
src/app/(app)/profile/edit.tsx
(src/app/(app)/(tabs)/ deleted entirely)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the four route trees out of (tabs) and fix import depths</name>
  <read_first>
    - src/app/(app)/_layout.tsx (currently `<SessionExpiryBanner />` + `<Stack screenOptions={{headerShown:false}}><Stack.Screen name="(tabs)" /></Stack>`)
    - src/app/(app)/(tabs)/_layout.tsx (the Tabs layout being deleted — 4 Tabs.Screen entries)
    - src/app/(app)/(tabs)/index.tsx (imports at depth `../../../`)
    - src/app/(app)/(tabs)/vehicles/_layout.tsx, index.tsx, new.tsx, [id].tsx (imports at depth `../../../../`)
    - src/app/(app)/(tabs)/verify/_layout.tsx, index.tsx, [documentType].tsx (imports at depth `../../../../`)
    - src/app/(app)/(tabs)/profile/_layout.tsx, index.tsx, edit.tsx (imports at depth `../../../../`)
  </read_first>
  <action>
Use `git mv` (this is a git repo — preserve history) for each file so renames are tracked:

```
git mv "src/app/(app)/(tabs)/index.tsx"   "src/app/(app)/index.tsx"
git mv "src/app/(app)/(tabs)/vehicles"    "src/app/(app)/vehicles"
git mv "src/app/(app)/(tabs)/verify"      "src/app/(app)/verify"
git mv "src/app/(app)/(tabs)/profile"     "src/app/(app)/profile"
git rm  "src/app/(app)/(tabs)/_layout.tsx"
```

After the moves the `(tabs)` directory must be empty and gone.

**Fix relative import depths — the directory depth drops by exactly one level for every moved
file.** Route groups like `(app)` and `(tabs)` are real directories on disk, so removing one
segment shortens every `../` chain by one:

| File (new path) | Old prefix | New prefix |
|---|---|---|
| `src/app/(app)/index.tsx` | `'../../../` | `'../../` |
| `src/app/(app)/vehicles/index.tsx` | `'../../../../` | `'../../../` |
| `src/app/(app)/vehicles/new.tsx` | `'../../../../` | `'../../../` |
| `src/app/(app)/vehicles/[id].tsx` | `'../../../../` | `'../../../` |
| `src/app/(app)/verify/index.tsx` | `'../../../../` | `'../../../` |
| `src/app/(app)/verify/[documentType].tsx` | `'../../../../` | `'../../../` |
| `src/app/(app)/profile/index.tsx` | `'../../../../` | `'../../../` |
| `src/app/(app)/profile/edit.tsx` | `'../../../../` | `'../../../` |

(`vehicles/_layout.tsx`, `verify/_layout.tsx`, `profile/_layout.tsx` import only from
`'expo-router'` — no relative imports to fix. Verify this rather than assuming.)

Concretely, the moved `src/app/(app)/index.tsx` imports become:
```
import { Badge } from '../../components/Badge';
import { Card } from '../../components/Card';
import { useKycStatusQuery } from '../../features/kyc/api';
import { useProfileQuery } from '../../features/profile/api';
```

**Rewrite `src/app/(app)/_layout.tsx` to:**
```tsx
import { Stack } from 'expo-router';

import { SessionExpiryBanner } from '../../features/auth/components/SessionExpiryBanner';

export default function AppLayout() {
  return (
    <>
      <SessionExpiryBanner />
      {/* No explicit <Stack.Screen> children on purpose: Expo Router auto-registers
          every sibling route file, so later plans can add /menu without touching this
          file. Every child screen already sets headerShown itself or inherits false. */}
      <Stack screenOptions={{ headerShown: false }} />
    </>
  );
}
```
Its own import stays at `'../../features/auth/components/SessionExpiryBanner'` — this file
did not move.

Do NOT change any screen's rendered JSX, business logic, or styling in this task. Route-literal
fixes are Task 2. The only edits here are file locations, `../` depths, and `(app)/_layout.tsx`.
  </action>
  <verify>
    <automated>test ! -d "src/app/(app)/(tabs)" &amp;&amp; test -f "src/app/(app)/index.tsx" &amp;&amp; test -f "src/app/(app)/vehicles/_layout.tsx" &amp;&amp; test -f "src/app/(app)/verify/[documentType].tsx" &amp;&amp; test -f "src/app/(app)/profile/edit.tsx" &amp;&amp; ! grep -rq "\.\./\.\./\.\./\.\./" "src/app/(app)" &amp;&amp; ! grep -q "Tabs" "src/app/(app)/_layout.tsx" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/(tabs)/` does not exist (`test ! -d "src/app/(app)/(tabs)"`)
    - All 12 target files listed in the AFTER tree exist
    - `grep -rc "\.\./\.\./\.\./\.\./" "src/app/(app)"` returns 0 matches (no four-level relative imports remain anywhere under `(app)`)
    - `grep -c "\.\./\.\./\.\./" "src/app/(app)/index.tsx"` returns 0 (Home is at two levels now)
    - `src/app/(app)/_layout.tsx` contains `<Stack screenOptions={{ headerShown: false }} />` and contains NO `Stack.Screen` and NO `Tabs`
    - `grep -rn "expo-router" "src/app/(app)/vehicles/_layout.tsx"` confirms it still imports Stack and is otherwise unchanged
    - No file under `src/app/(app)/` contains the string `(tabs)/_layout`
  </acceptance_criteria>
  <done>The `(tabs)` group is deleted, all 12 route files sit directly under `src/app/(app)/`, every relative import resolves at the new depth, and `(app)/_layout.tsx` renders a bare Stack with no explicit children.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite all 10 stale (tabs) route literals to their flattened form</name>
  <read_first>
    - src/app/(app)/index.tsx (3 literals: '/(app)/(tabs)/verify', '/(app)/(tabs)/vehicles', '/(app)/(tabs)/profile')
    - src/app/(app)/verify/index.tsx (3 literals: two `{ pathname: '/(app)/(tabs)/verify/[documentType]' }`, one '/(app)/(tabs)/vehicles/new')
    - src/app/(app)/vehicles/index.tsx (2 literals: '/(app)/(tabs)/vehicles/new' and the template string `/(app)/(tabs)/vehicles/${item.id}`)
    - src/app/(app)/vehicles/[id].tsx (2 literals: two `{ pathname: '/(app)/(tabs)/verify', params: { vehicleId: vehicle.id } }`)
    - src/features/vehicles/components/VehicleListEmptyState.tsx (1 literal: '/(app)/(tabs)/vehicles/new')
    - src/features/profile/components/ProfileView.tsx (1 literal: '/(app)/(tabs)/profile/edit')
    - .planning/phases/02-online-offline-foreground-location-maps/02-RESEARCH.md ("Anti-Patterns to Avoid" — use the FLATTENED form, not a new group-qualified form; RESEARCH.md undercounted these call sites as 2, the real count is 10 across 6 files)
  </read_first>
  <action>
Replace every `'/(app)/(tabs)/X'` href with `'/X'`. Route groups never appear in the URL, so
the flattened form already resolves today and continues to resolve after the restructure.
Do NOT substitute a new group-qualified form like `'/(app)/vehicles'` — use the bare
flattened path.

Exact replacement table (10 sites, 6 files):

| File | Before | After |
|---|---|---|
| `src/app/(app)/index.tsx` | `router.push('/(app)/(tabs)/verify')` | `router.push('/verify')` |
| `src/app/(app)/index.tsx` | `router.push('/(app)/(tabs)/vehicles')` | `router.push('/vehicles')` |
| `src/app/(app)/index.tsx` | `router.push('/(app)/(tabs)/profile')` | `router.push('/profile')` |
| `src/app/(app)/verify/index.tsx` | `pathname: '/(app)/(tabs)/verify/[documentType]'` (x2) | `pathname: '/verify/[documentType]'` |
| `src/app/(app)/verify/index.tsx` | `router.push('/(app)/(tabs)/vehicles/new')` | `router.push('/vehicles/new')` |
| `src/app/(app)/vehicles/index.tsx` | `router.push('/(app)/(tabs)/vehicles/new')` | `router.push('/vehicles/new')` |
| `src/app/(app)/vehicles/index.tsx` | `` router.push(`/(app)/(tabs)/vehicles/${item.id}`) `` | `` router.push(`/vehicles/${item.id}`) `` |
| `src/app/(app)/vehicles/[id].tsx` | `pathname: '/(app)/(tabs)/verify'` (x2, both keep `params: { vehicleId: vehicle.id }`) | `pathname: '/verify'` |
| `src/features/vehicles/components/VehicleListEmptyState.tsx` | `router.push('/(app)/(tabs)/vehicles/new')` | `router.push('/vehicles/new')` |
| `src/features/profile/components/ProfileView.tsx` | `router.push('/(app)/(tabs)/profile/edit')` | `router.push('/profile/edit')` |

`src/features/profile/logout.ts` already uses `router.replace('/(auth)/login')` — the `(auth)`
group is untouched by this plan, leave it exactly as-is.

**Then regenerate the typed-routes declaration file.** This is mandatory, not optional:
`.expo/types/router.d.ts` currently declares only `(tabs)`-qualified paths, and it is
regenerated ONLY by the `expo start` dev-server file watcher — not by `tsc`, `expo export`, or
`expo lint`. This exact pitfall was already hit and logged in STATE.md during Phase 01.1. Run:

```
npx expo start --clear
```

Leave it running until the terminal prints the dev-server ready banner and
`.expo/types/router.d.ts` has been rewritten (watch for its mtime to change), then stop it with
Ctrl-C. Confirm the regenerated file no longer mentions `(tabs)` before moving on — if it
still does, `expo start` did not complete its route scan and must be re-run.

Only after that does `npx tsc --noEmit` become a meaningful signal.
  </action>
  <verify>
    <automated>! grep -rq "(tabs)" src &amp;&amp; ! grep -q "(tabs)" .expo/types/router.d.ts &amp;&amp; grep -q "'/menu'\|/verify" .expo/types/router.d.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npx expo lint &amp;&amp; npx jest --watchAll=false &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "(tabs)" src` returns 0 matches across the entire `src` tree
    - `grep -c "(tabs)" .expo/types/router.d.ts` returns 0
    - `.expo/types/router.d.ts` contains `` `/vehicles/new` ``, `` `/verify/[documentType]` ``, and `` `/profile/edit` `` as non-group-qualified path literals
    - `src/features/profile/logout.ts` still contains `router.replace('/(auth)/login')` (unchanged)
    - `npx tsc --noEmit` exits 0
    - `npx expo lint` exits 0
    - `npx jest --watchAll=false` exits 0 with no new failures versus the pre-plan baseline
  </acceptance_criteria>
  <done>Zero `(tabs)` references remain anywhere in `src` or in the regenerated typed-routes file, and tsc/lint/jest are all green.</done>
</task>

</tasks>

<verification>
Full-suite gate from 02-VALIDATION.md, plus the android export that proves the route tree
bundles:

```
npx tsc --noEmit && npx expo lint && npx jest --watchAll=false && npx expo export --platform android
```

Also sanity-grep that no orphaned tab-bar code survives:
```
grep -rn "Tabs\b" src   # must return nothing
```
</verification>

<success_criteria>
- `src/app/(app)/(tabs)/` no longer exists and `Tabs` appears nowhere in `src`
- All 12 route files live directly under `src/app/(app)/`
- Zero four-level (`../../../../`) relative imports remain under `src/app/(app)/`
- All 10 `(tabs)`-qualified route literals are rewritten to their flattened form
- `.expo/types/router.d.ts` has been regenerated by `expo start` and contains no `(tabs)` segment
- `npx tsc --noEmit && npx expo lint && npx jest --watchAll=false && npx expo export --platform android` is green
</success_criteria>

<output>
After completion, create `.planning/phases/02-online-offline-foreground-location-maps/02-02-SUMMARY.md`.
Must record: the final route tree, the fact that `expo start` was run to regenerate
`.expo/types/router.d.ts` (and must be re-run by any later plan that adds a new route file
before `tsc` will accept a push to it), and that `(app)/_layout.tsx` now has no explicit
`<Stack.Screen>` children so new sibling routes auto-register.
</output>
